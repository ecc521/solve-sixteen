# Optional. Defaults to the deployed Worker if unset.
# Point at http://localhost:8787 when running `npm run dev` inside ../worker.
VITE_API_BASE=https://solve-sixteen-api.tuckerwillenborg.workers.dev
